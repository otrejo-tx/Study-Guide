### Study Guide

**1)** BST Structure

```python
struct treeNode //now defining
	{
		int _key;
		pTreeNode _left;  //treeNode *_left
		pTreeNode _right; //treeNode *_right
		treeNode(int key) : _key(key), _left(nullptr), _right(nullptr) {}
	};

	pTreeNode _root;
```

### Answer 1

```C++
```
